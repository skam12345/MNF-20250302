using Newtonsoft.Json;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SceneManagement;

public class Problem : MonoBehaviour
{


	//private Dictionary<string, StageClass> stageDict;
	//private List<StageClass> stageList = new List<StageClass>();

	public void Awake()
	{
		//	TextAsset jsonFile = Resources.Load<TextAsset>("Jsons/StageJson");
		//	if (jsonFile != null)
		//	{
		//		stageDict = JsonConvert.DeserializeObject<Dictionary<string, StageClass>>(jsonFile.text);
		//		foreach (string key in stageDict.Keys)
		//		{
		//               StageClass data = stageDict[key];
		//			stageList.Add(data);
		//		}
		//	}
		//}
	}
}